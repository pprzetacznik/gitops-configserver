tenants:
- tenant1
- tenant2
repositories:
  test_repo1:
    type: local
    url: local_path
  gitops-configserver-tests:
    type: git
    url: git@github.com:pprzetacznik/gitops-configserver-tests.git
hierarchy:
- nodes/{node}.yaml
- environments/{environment}.yaml
- branches/{branch}.yaml
- defaults.yaml
